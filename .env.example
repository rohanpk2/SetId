# Database (use localhost when running uvicorn on your machine; Docker Compose overrides
# DATABASE_URL to host `db` — see docker-compose.yml)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/wealthsplit

# JWT Auth
JWT_SECRET_KEY=change-me-to-a-random-secret-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Stripe
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Stripe Issuing — virtual cards for bill payment
# Set to true to use real Stripe Issuing API (requires Issuing-enabled Stripe account)
STRIPE_ISSUING_ENABLED=false

# Feature flags
# Enable virtual card creation flow (must also set STRIPE_ISSUING_ENABLED for real cards)
FEATURE_VIRTUAL_CARDS=false

# Environment: "development" or "production"
# In production: docs are disabled, default JWT secret is rejected at boot
ENVIRONMENT=development

# Groq
GROQ_API_KEY=gsk-your-groq-api-key
GROQ_BASE_URL=https://api.groq.com/openai/v1
GROQ_RECEIPT_VISION_MODEL=meta-llama/llama-4-scout-17b-16e-instruct
GROQ_RECEIPT_CLEANUP_MODEL=openai/gpt-oss-20b

# File uploads
UPLOAD_DIR=./uploads

# CORS (JSON array of allowed origins)
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# Twilio SMS (payment requests & reminders; use SMS_DEV_MODE=true locally without sending)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
PUBLIC_PAYMENT_BASE_URL=https://app.wealthsplit.com
SMS_DEV_MODE=true
SMS_MAX_RETRIES=3
SMS_RETRY_BASE_DELAY_SEC=0.5
REMINDER_UNPAID_AFTER_HOURS=24
REMINDER_MIN_INTERVAL_HOURS=24
INTERNAL_JOB_SECRET=
REMINDER_JOB_INTERVAL_SEC=3600
# Twilio Verify (SMS OTP). Optional if OTP_DEV_MODE=true.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=

# Use in-memory OTP when true OR when Twilio vars are unset (logs code to server logs).
OTP_DEV_MODE=true

# Apple Sign In
APPLE_TEAM_ID=your_apple_team_id
APPLE_KEY_ID=your_apple_key_id
APPLE_BUNDLE_ID=com.yourcompany.wealthsplit
APPLE_PRIVATE_KEY_PATH=./apple_private_key.p8
