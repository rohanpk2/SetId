# Database (use localhost when running uvicorn on your machine; Docker Compose overrides
# DATABASE_URL to host `db` — see docker-compose.yml)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/wealthsplit

# JWT Auth
JWT_SECRET_KEY=change-me-to-a-random-secret-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Stripe
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# File uploads
UPLOAD_DIR=./uploads

# CORS (JSON array of allowed origins)
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# Twilio Verify (SMS OTP). Optional if OTP_DEV_MODE=true.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=

# Use in-memory OTP when true OR when Twilio vars are unset (logs code to server logs).
OTP_DEV_MODE=true

# Apple Sign In
APPLE_TEAM_ID=your_apple_team_id
APPLE_KEY_ID=your_apple_key_id
APPLE_BUNDLE_ID=com.yourcompany.wealthsplit
APPLE_PRIVATE_KEY_PATH=./apple_private_key.p8
